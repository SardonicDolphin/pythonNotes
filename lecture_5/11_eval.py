# eval evaluates an expression.
# Doesn't sound so bad.
# But, what can be an expression?

expression = input("Please enter just an expression: ")
print("Evaluating:\n" + expression)
eval(expression)
