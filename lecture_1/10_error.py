from time import sleep

# Except for syntax errors, most semantic errors are only caught during runtime,
# which can waste a lot of time if the error takes a while to appear.

sleep(30)
print(undeclared)
