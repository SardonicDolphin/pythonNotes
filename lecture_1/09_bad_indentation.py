# This program will not run, because the indentation is not consistent,
# even though it looks alright.
# This problem generally only happens when two programmers
# use different editor settings for the same file.

if (__name__ == "__main__"):
	print("Uses tab")
        print("Uses spaces")
